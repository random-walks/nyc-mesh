# Quickstart CityGML

This is the smallest end-to-end `nyc-mesh` example.

It uses the packaged sample CityGML file and writes:

- `artifacts/buildings.geojson`
- `artifacts/buildings.parquet`
- `reports/quickstart-citygml-tearsheet.md`
