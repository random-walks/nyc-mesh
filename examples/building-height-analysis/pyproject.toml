[project]
name = "nyc-mesh-building-height-analysis"
version = "0.0.0"
description = "Building height and terrain export example for nyc-mesh."
requires-python = ">=3.11"
dependencies = ["nyc-mesh"]

[tool.uv.sources]
nyc-mesh = { path = "../..", editable = true }
