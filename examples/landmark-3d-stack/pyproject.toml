[project]
name = "nyc-mesh-landmark-3d-stack"
version = "0.0.0"
description = "Real landmark-scale 3D stack example for nyc-mesh."
requires-python = ">=3.11"
dependencies = ["nyc-mesh"]

[tool.uv.sources]
nyc-mesh = { path = "../..", editable = true }
