[project]
name = "nyc-mesh-neighborhood-clip"
version = "0.0.0"
description = "Neighborhood clip example for nyc-mesh."
requires-python = ">=3.11"
dependencies = ["nyc-mesh"]

[tool.uv.sources]
nyc-mesh = { path = "../..", editable = true }
